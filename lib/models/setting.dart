class Setting {
  String? id;
  // String heroTag;
  // dynamic param;
  // dynamic goBackRoute;

  Setting(
    {
      this.id, 
      //this.heroTag, this.param, this.goBackRoute
    }
  );

  // @override
  // String toString() {
  //   return '{id: $id, heroTag:${heroTag.toString()}}';
  // }
}
